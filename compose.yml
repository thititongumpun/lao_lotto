services:
  lotto:
    image: thititongumpun/lao_lotto:latest
    ports:
      - "8000:8000"
    environment:
      TZ: Asia/Bangkok
      LOTTO_DB_URL: ${LOTTO_DB_URL}
    env_file:
      - .env
    restart: unless-stopped